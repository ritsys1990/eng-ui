export const COMPONENT_NAME = 'AddClient';

export const Constants = Object.freeze({
  createClientStorageRefreshTime: 20000,
  defaultMemberFirmCode: 'US',
});

export const FormErrorModel = Object.freeze({
  client: null,
  fiscalYear: null,
  country: null,
});
