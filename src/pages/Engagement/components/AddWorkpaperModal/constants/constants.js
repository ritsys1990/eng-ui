export const COMPONENT_NAME = 'AddWorkpaper';

export const Constants = Object.freeze({
  GetClientRefreshTimeout: 4000,
  defaultMemberFirmCode: 'US',
});

export const FormErrorModel = Object.freeze({
  client: null,
  fiscalYear: null,
  country: null,
});
